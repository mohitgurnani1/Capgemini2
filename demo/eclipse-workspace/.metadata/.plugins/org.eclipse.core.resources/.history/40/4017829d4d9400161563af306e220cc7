import  org.junit.Assert;
// if we import static org.junit.Assert.fail, then we can import specific static methods and specific static variables as well. It will be equivalent to c functions. Simply we can call fail() and assertSame();

import org.junit.Assert;
import org.junit.Ignore;
import org.junit.Test;

public class TestStringSample {

	@Test
	@Ignore
	public void test() {
		Assert.fail("Not yet implemented");
	}
	@Test
	public void someString(){
		String msg1=new String("hello");
		String msg2="hello";
		String msg3="hello";
		//Assert.assertEquals(msg1,msg3);
		//Assert.assertSame(msg1, msg2);
	Assert.assertSame(msg3, msg2);	
	}
	
	
	
}
